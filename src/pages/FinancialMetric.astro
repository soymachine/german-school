---
import NextButton from '../components/buttons/NextButton.astro';
import QuestionHeader from '../components/QuestionHeader.astro';
import Punctuation from '../components/Punctuation.astro';

interface Props {
	step: string;
}
import { imageURL } from '../../public/scripts/helpers/ImageURL';
// const companiesBorderURL = imageURL("imgs/companies/border/companies-border.png")


const { step } = Astro.props;

---
<div class=`content-holder`>
    <div class="info">
        <div class=`title-step-${step}`>
            <QuestionHeader character="sarah">
                Also, I want to know how well can you transform <strong>investments into profits.</strong>
            </QuestionHeader>   
        </div>
    </div>
    <div class="info-holder">
        <div id="question-holder">
            <div class="info-question">
                <div class="question-text">What is the financial metric that tracks profit relative to shareholder's equity?</div>
                <div class="answers">
                    <a class=`answer btn-step-${step}-option` id=`step-${step}-answer-button-1` href="#">ROE</a>
                    <a class=`answer btn-step-${step}-option` id=`step-${step}-answer-button-2` href="#">P/E</a>
                    <a class=`answer btn-step-${step}-option` id=`step-${step}-answer-button-3` href="#">ROI</a>
                </div>
            </div>
        </div>
        <div id="result-holder">
            <div class="result-holder">
                <div class="visual-result">
                    <div class="visual-result-left">

                    </div>
                    <div class="visual-result-center">
                        <img id="visual-result-image" src="./imgs/financial-metrics/icon-result.png">
                    </div>
                    <div class="visual-result-right">
                        <img id="visual-result-mark" src="./imgs/correct.png">
                    </div>
                </div>
                <div class="result-title">ROE</div>                
            </div>
        </div>        
    </div>
    <Punctuation step={step}/>
    <div class="next-button-wrapper">
        <NextButton step={step}/>
    </div>
</div>
<style>
    .content-holder {
        position:relative;
        --text-blue:#14519C; 
        --color-blue:#044596;
        color:darkblue;
        text-align: center;
        font-size: 14px;
        margin:20px 10px;
        font-family: 'Roboto', sans-serif;
        overflow: hidden;
    }

    h1 {
        font-size: 15px;
        font-weight: 400;
    }

    .info-holder {
        display: grid;
        grid-template-columns: 100% 100%;
        gap:10px;
        margin-bottom: 30px;
        /*transform: translateX(-328px);*/
    }   

    .info-holder > div{
        /*border:1px solid red;*/
    }

    .info-grid{
        display: grid;
        grid-template-columns: 1fr auto 1.5fr;
        grid-template-areas: 'picture wedge text';
        margin-bottom: 20px;
    }

    .info-picture{
        grid-area: picture;
        display: flex;
    }

    .info-picture img{
        align-self: flex-end;
    }

    .info-wedge{
        grid-area: wedge;
    }

    .info-wedge img{
       position: relative;
       top: 40%;
       left:2px;
    }

    .info-text{
        grid-area: text;
        border: 2px solid #C1D1E5;
        border-radius: 2px;
        padding: 15px;
    }

    .info-question{
        grid-area: question;
        margin: 0px 25px;
    }

    .question-text {
        padding:10px;
        color:#054696;
    }

    .answers {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-top: 20px;
    }

    .answers .btn-step-option-selected{
        background-color:var(--color-blue);
        color:white;
    }

    .answer {
        background-color: white;
        border:2px solid var(--color-blue);
        border-radius: 8px;
        color:var(--color-blue);
        font-size: 20px;
        padding:10px;
        text-decoration: none;
    }

    .result-holder {
        display: flex;
        flex-direction: column;
        margin: 0px 30px;
    }

    .visual-result {
        display: flex;
        flex-direction: row;
        justify-content:center;
        margin-bottom: 10px;
    }

    .visual-result-left{
        width: 20%;
    }

    .visual-result-center{
        width: auto;
        padding: 10px 0px;
    }

    .visual-result-center img {
        width: 75%;
    }

    .visual-result-right{
        width: 20%;
        align-self: center;
    }

    .result-title {
        background-color: #C9EBD4;
        font-size:20px;
        font-color:var(--color-blue);
        border-radius:10px;
        padding:10px 0px;
        margin-bottom: 10px;
        font-weight: bold;
        border:2px solid #044596;
    }

    .result-title-wrong {
        background-color: #FFD8DB !important;
    }

    .result-sentence {
        font-size:17px;
        margin-bottom: 30px;
    }

    /* In Css how are all the paragraphs that descend from .result-sentence? */
    .result-paragraph  {
        padding:0px;
        margin:0px;
    }

    /*
    .user-name {
        color:#EB590A;
    }
    */

    .result-description{
        text-align: left;
    }

    .next-button-wrapper{
        padding-top: 20px;
    }


</style>